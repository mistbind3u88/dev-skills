---
name: doc-check
description: コード変更に対して関連ドキュメントが整合しているかをチェックする。不整合があれば報告のみ行い、修正はスキル /doc-sync で行う。
allowed-tools: Bash(git diff:*) Bash(git log:*) Bash(git status:*) Read Glob Grep
---

# doc-check スキル

コード変更に対して関連ドキュメントが追従しているかをチェックし、結果を報告する。修正は行わない。

## 手順

### 1. 変更内容を把握する

```bash
git diff --name-only main..HEAD
git log --oneline main..HEAD
```

変更されたファイルの一覧とコミット履歴を確認する。

### 2. 関連ドキュメントを特定する

変更内容から、影響を受ける可能性のあるドキュメントを特定する。

対象ドキュメント:

- `AGENTS.md` / `CLAUDE.md` — リポジトリ全体の規則・制約
- `**/SKILL.md` — スキル定義（手順・allowed-tools・description）
- `**/AGENTS.md` — スキル固有の補足指示
- `README.md` — プロジェクト概要

特定の基準:

- 変更されたファイルと同じディレクトリのドキュメント
- 変更内容が言及されているドキュメント（Grep で検索）
- スキルの allowed-tools や手順に影響するコード変更

### 3. 不整合をチェックする

各ドキュメントを読み、以下の観点で不整合がないか確認する。

| 観点                   | チェック内容                                                                                                                                                                                                                                          |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 参照の有効性           | ドキュメントが参照しているファイル・関数・コマンドが存在するか                                                                                                                                                                                        |
| 手順の正確性           | SKILL.md に記載された手順が現在のコードと一致するか                                                                                                                                                                                                   |
| allowed-tools の過不足 | スキルが使用するコマンドが allowed-tools に含まれているか、不要なものが残っていないか                                                                                                                                                                 |
| description の正確性   | スキルの description が現在の機能を正しく反映しているか                                                                                                                                                                                               |
| 削除済みコードへの言及 | 削除されたファイル・関数・設定への参照が残っていないか                                                                                                                                                                                                |
| レビュー誤指摘の予防   | コードを読むだけでは誤りに見える設計判断（暗黙の副作用への依存、意図的な省略、他リポジトリとの整合など）がコメントやドキュメントで説明されているか                                                                                                    |
| 記述スタイル           | `AGENTS.md` / `CLAUDE.md` がコードの逐語説明や実装詳細の羅列になっていないか。コードを読めば分かる内容は省き、制約・判断基準・背景など「コードからは読み取れない情報」中心でコンパクトに保たれているか。判定基準の詳細は `doc-check/AGENTS.md` を参照 |

### 4. 結果を報告する

- 不整合がない場合: 成功として報告する
- 不整合がある場合: 失敗として一覧を報告する

```
ドキュメント整合性チェック:
  commit/SKILL.md:  allowed-tools に不要な Bash(git branch:*) が残っている
  check/SKILL.md:   ステップ2a の例が現在のプロジェクト構成と合っていない
```

## 注意

- このスキルはチェックと報告のみを行う。ドキュメントの修正はスキル `/doc-sync` で行う
- `/check` から呼び出された場合、不整合があれば check 全体を失敗にする
